/**
 * Public API surface for react-native-intercepting-webview.
 *
 * Re-exports the `InterceptingWebView` component and its associated types.
 * The component is Android-first and falls back to a plain View on iOS.
 */
export { default as InterceptingWebView } from './InterceptingWebView';
export * from './InterceptingWebView';
