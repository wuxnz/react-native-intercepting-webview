pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{js,ts,jsx,tsx}"
      run: npx eslint {staged_files} || true
    types:
      glob: "*.{js,ts,jsx,tsx}"
      run: npx tsc || true
commit-msg:
  parallel: true
  commands:
    commitlint:
      run: npx commitlint --edit || true
